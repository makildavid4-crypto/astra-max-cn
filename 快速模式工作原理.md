# 去中心化留言板 DApp 工作原理讲解

## 一、总览：DApp 到底在做什么？

去中心化留言板 DApp 核心就是两件事：

1. **读链上数据**：从区块链（本地 Hardhat 节点）把所有留言读出来，显示在页面上。
2. **写链上数据（发交易）**：用户写一条留言 → 构造成一笔交易 → **签名** → 发给节点 → 节点打包上链 → 留言永久保存。

这里面**签名**是最关键的一步，因为**区块链只认签名不认人**：只有用某个账户的私钥对交易签名，节点才会认这笔交易是「该地址发的」，才会扣它的 ETH 作为 gas 并执行合约。

两种模式（MetaMask 模式 / 快速模式）的区别，**本质就是"谁来持有私钥、谁来签名"**。

---

## 二、传统「MetaMask 模式」流程（签名在 MetaMask 内部做）

```
你的浏览器页面（index.html）
    │ 发请求：我要一笔交易签名
    ▼
window.ethereum（MetaMask 注入的代理对象）
    │ 弹窗问用户："DApp 要发交易，同意吗？"
    │ 用户点"确认"
    ▼
MetaMask 扩展内部（私钥存在这里，永远不会给页面）
    │ 用它保存的私钥对交易进行签名
    ▼
MetaMask 把「已签名的交易」返回给页面
    │
    ▼
页面把已签名交易发给本地节点 127.0.0.1:8545
    │ 节点验证签名 → 打包 → 上链
    ▼
留言永久保存
```

**核心特点：私钥在 MetaMask 手里，页面永远碰不到私钥**（安全），代价是必须通过 MetaMask 注入的 `window.ethereum` API 走流程 —— 也就是容易卡在「弹窗授权」这一步。

---

## 三、「快速模式」流程（签名在页面里用 Hardhat 测试私钥做）

```
你的浏览器页面（index.html）
    │
    │ 1. 代码里写死了 Hardhat 默认的 10 个测试私钥（本地专用）
    │ 2. 用 ethers.Wallet(私钥, provider) 在页面临时生成一个签名对象
    │ 3. 用户点"上链留言" → 页面直接用这个 Wallet 对象对交易签名
    ▼
页面内部完成签名（私钥在页面内存里，签名后就丢）
    │
    ▼
页面把「已签名的交易」直接通过 HTTP RPC 发给 127.0.0.1:8545
    │ 节点验证签名 → 打包 → 上链
    ▼
留言永久保存
```

**核心特点：页面直接通过 HTTP 连节点，并在页面内用 Hardhat 测试私钥完成签名**——**完全绕开了 `window.ethereum` / MetaMask 注入机制**，所以不会碰到弹窗、授权、扩展冲突等任何问题。

---

## 四、快速模式每一步具体做什么（代码对应）

对应 `frontend/index.html`：

### ① 启动快速模式（`connectQuick()` 函数）

```js
provider = new ethers.JsonRpcProvider("http://127.0.0.1:8545");
```

- 创建一个 **JSON-RPC Provider**。这是 ethers.js 里代表「和区块链节点通信的通道」的对象，相当于"链上数据的 HTTP 客户端"。
- Provider 是**只读的**：只负责读区块 / 读合约数据 / 广播「已经签好名」的交易；它自己不会签名。

```js
const net = await provider.getNetwork();
if (net.chainId === 31337n) /* 验证节点是否是本地 Hardhat 网络 */
```

- 校验连上的是不是本地 Hardhat 网络（chainId=31337），防止误连其他网络。

### ② 选账户并生成 Signer（`applyQuickUser()` 函数）

```js
const HARDHAT_KEYS = [
  "0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80", // 账户 #0 私钥
  "0x59c6995e998f97a5a0044966f0945389dc9e86dae88c7a8412f4603b6b78690d", // 账户 #1 私钥
  ...
];
signer = new ethers.Wallet(HARDHAT_KEYS[idx], provider);
currentAccount = signer.address;  // 0xf39F...
```

- `HARDHAT_KEYS`：执行 `npx hardhat node` 时，Hardhat 默认生成并打印的 **20 个公开测试账户私钥**，每个账户初始有 10000 ETH（仅本地开发用，严禁主网使用这些私钥）。
- `ethers.Wallet(私钥, provider)`：生成一个 **Wallet（Signer）签名对象**——这个对象「持有私钥」、能对交易做 ECDSA 签名。
- 从私钥可以推导出公钥，再推导出地址 `signer.address`，所以不需要额外传地址。

### ③ 把签名对象「绑定」到合约实例

```js
contract = getContract().connect(signer);
```

- `getContract()` 返回的 `ethers.Contract` 默认是**只读**的，只能调用 `getAllMessages()` 这类 `view/pure` 函数。
- `.connect(signer)` 返回一个**和同一个合约地址绑定、但会用 signer 来签名发交易**的新合约实例。此后调用：
  ```js
  contract.postMessage("你好")
  ```
  ethers.js 会自动：
  1. 构造「调用 MessageBoard.sol 的 postMessage 函数、参数为「你好」」的交易；
  2. 用 signer 持有的私钥对交易做 ECDSA 签名；
  3. 把签名后的交易字节码通过 provider（HTTP POST 到 127.0.0.1:8545）广播给节点；
  4. 等待节点把交易打包进区块 → 通过 `tx.wait()` 返回交易 receipt（含区块号等信息）。

### ④ 切换用户的本质（`userSelect` 的 change 事件）

「模拟用户」下拉框选择 #1、#2 时，本质就是**换一个私钥重新生成 Wallet Signer，再重新 `.connect()` 绑定到合约**。因为 Hardhat 节点上每个默认账户都有 10000 ETH，所以 #1、#2 都能独立付 gas 发交易，对应留言列表里的「作者地址」会变成不同的 `0x7099...`、`0x3C44...`——完美模拟"多用户"。

---

## 五、快速模式与 MetaMask 模式对比

| 维度 | MetaMask 模式 | 快速模式（JsonRpcProvider + Wallet） |
|------|---------------|--------------------------------------|
| **私钥在哪** | MetaMask 扩展沙盒内，页面拿不到 | 页面临时内存里（用完即丢） |
| **签名位置** | MetaMask 内部签名 | 页面内 ethers.Wallet 签名 |
| **节点连接** | 页面 → `window.ethereum` → MetaMask → 节点 | 页面直接 HTTP 连 `127.0.0.1:8545` |
| **用户体验** | 每次发交易都要 MetaMask 弹窗确认 | 直接发，0 确认（适合开发） |
| **适用场景** | 真实生产 DApp（用户自己的钱包） | 本地开发 / 演示 / 自动化测试 |
| **安全要求** | ✅ 生产安全 | ❌ 仅本地/测试网能用，私钥写死在前端绝对不能上主网 |
| **上链结果** | 完全一样（交易都会被节点打包进区块） | 完全一样（签名算法相同，节点不认来源只认签名） |

---

## 六、一句话总结

> 快速模式的本质是：**既然本地 Hardhat 节点已经给我们提供了带 ETH 的测试账户私钥，我们就直接让前端用这些私钥在页面内完成交易签名，再通过标准的 JSON-RPC HTTP 接口把签名后交易发给节点。这样整个流程完全不依赖浏览器扩展（MetaMask）注入的 `window.ethereum`，也就绕开了所有「扩展冲突 / 弹窗不出来 / 授权死锁」等浏览器环境问题——但签名算法、上链结果、合约调用、区块确认，和真实 MetaMask 发送是 100% 等价的，完全满足去中心化留言板 DApp「链上发布留言、多用户切换、永久保存不可篡改」的任务要求。
