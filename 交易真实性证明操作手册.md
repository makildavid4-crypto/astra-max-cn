# 交易真实性证明 · 操作手册

> 回答「怎么证明这笔交易真的发生了？节点凭什么认这笔交易是该地址发的、真的扣了它的 ETH？」
> 本文提供 **前端可视化证明** + **命令行证明** 两套流程。

---

## 一、为什么「看账户剩余 ETH」能证明交易真实发生？

区块链节点处理一笔交易的流程是：

1. 节点收到「签名后的交易」，用 **ecrecover(交易哈希, v,r,s)** 还原出签名者公钥 → 得到地址 `from`；
2. 如果还原出的 `from` 不等于交易里声明的 `from` → **直接拒绝**（非法签名）；
3. 如果 `from` 账户余额 < 预估 gas 费 → **拒绝**（付不起手续费）；
4. 只有以上验证都通过，节点才会：
   - 先**预扣该账户的 gas（gasLimit × gasPrice）**；
   - 执行合约的 `postMessage()` 函数；
   - 按实际消耗的 `gasUsed` 结算 gas 费（没用完的 gas 退给 `from`）；
   - 把执行成功的交易和合约事件打包进区块。

**结论：只要看到该地址的 ETH 真的变少了（且差值正好是 gasUsed × gasPrice），就证明步骤 1~4 全通过了——也就是签名验证通过、节点认这笔交易、合约真的执行了。**

但这只是**间接证明**，下面提供 4 层更直接的证明（任意一层都能独立证明）。

---

## 二、方式 A：DApp 页面上的「可视化证明」（最直观，讲述演示用）

### A1. 看「账户 ETH」变化量 = gas 费

1. 打开 `http://127.0.0.1:3000/` → 点【⚡ 快速模式（无需 MetaMask）】；
2. 记下「账户 ETH」显示的初始余额（例如 `10000.000000 ETH`）；
3. 输入留言 → 点【上链留言】→ 交易成功后，页面会立刻显示：
   - 💰 **「交易后变化 -0.0000xxxx ETH = xxxxx wei，即 Gas 费扣除」**（红色负数就是被扣的 gas）；
   - 同时下方会有一个**交易哈希链接**（0x...），点它直接跳区块浏览器。
4. **关键对照**：下方 txReceipt 显示的 `gasUsed × gasPrice(Gwei 转 ETH)` ≡ 上方余额差值，证明节点真的按标准 gas 公式扣了这个账户的 ETH。

### A2. 看交易 Receipt 显示「区块号 / gasUsed / 事件数」

每次发完留言，在「✅ 交易已上链」框里会显示：

```
区块 #5 · gas 消耗 64219 · 状态成功(✓) · 合约事件 1 条
```

- `区块 #N`：交易真的被打包进链了（不是本地缓存模拟）；
- `状态成功 ✓`：合约执行成功，留言真的被写入 storage；
- `合约事件 1 条`：`MessagePosted` 事件真的被发出（事件写入区块收据，不可删除）。

### A3. 用「🔍 区块浏览器」查询（最完整）

在留言板点「🔍 区块浏览器」或直接访问 `http://127.0.0.1:3000/explorer.html`：

1. **查交易（粘贴 tx hash）** 会展示三层信息：
   - 交易本身（from / to / value / nonce / data…）；
   - 交易 Receipt（status / gasUsed / logs / 实际 gas 费扣除）；
   - 合约事件日志（直接解析出 `MessagePosted` 的 author / content / timestamp / index，证明留言内容确实在链上）。
2. **核心证明区「签名者地址恢复」**：
   页面会用**和节点完全相同的算法** `ecrecover(交易哈希, v,r,s)` 还原出签名者地址，然后和交易 `from` 地址对比：
   - 绿色「✅ 完全一致」= 证明这条交易是该账户私钥签的，节点身份验证通过；
   - 还会显示「实际从 `from` 地址扣除的 gas 费 = x ETH」，扣 gas 和身份验证是**同一因果链**上的两件事。
3. **查区块**：输入区块号能看到该区块所有交易列表；
4. **查地址**：输入任意地址能看到余额 / nonce / 是否是合约 / code 内容。

> 从留言板点 tx 哈希链接会自动跳转区块浏览器并带 `?tx=0x...` 参数，页面会自动加载该交易，无需手动粘贴。

---

## 三、方式 B：Hardhat 控制台命令证明（更权威，Node 侧视角）

在项目根目录 `c:\Users\30368\Desktop\Dapp` 打开**新终端**（必须保持 `npx hardhat node` 那个终端别关），执行：

```bash
npx hardhat console --network localhost
```

进入交互式 `>` 控制台后，照着下面命令逐条敲演示即可。

### B1. 查余额（对应证明 A1：ETH 确实少了）

```js
// 0xf39F... 是 Hardhat 账户 #0（你用来发第一条留言的账户）
const addr = "0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266";
const bal = await ethers.provider.getBalance(addr);
ethers.formatEther(bal)   // 返回类似 "9999.999934752"，不再是 10000 → 证明确实被扣了 gas
```

### B2. 查某个交易的 Receipt（对应证明 A2）

先把一条 tx hash（例如留言板交易成功后给的 0x...）替换进去：

```js
const txHash = "0x..............（填你的交易哈希）";
const receipt = await ethers.provider.getTransactionReceipt(txHash);
receipt.status              // 1 = 成功, 0 = 失败
receipt.blockNumber         // 打包进了第几个区块
receipt.from                // 就是 gas 扣费方地址
receipt.gasUsed.toString()  // 实际 gas 消耗
receipt.logs.length         // 合约事件数量
receipt.logs[0].topics      // 事件 topic 里会有作者地址（已索引）
```

### B3. 查合约 storage 最底层：留言数组到底有没有变（终极证明链状态已改）

```js
const { abi } = require("./artifacts/contracts/MessageBoard.sol/MessageBoard.json");
const cb = new ethers.Contract("0x5FbDB2315678afecb367f032d93F642f64180aa3", abi, ethers.provider);
await cb.getCount();           // 留言总数（应该等于你发布的次数）
await cb.getAllMessages();     // 完整读取所有留言
await cb.getMessage(0);        // 读取索引 0 的留言（author, content, timestamp）

// 也可以直接在 storage 槽位上读（真正的链上 16 进制存储，最底层）
await ethers.provider.getStorageAt(await cb.getAddress(), 3);
// 合约定义顺序是 messages(0) / messageCountOf(1) / totalMessages(2) ...
// 读第 2 槽位 = totalMessages
await ethers.provider.getStorageAt(await cb.getAddress(), 2);
// 返回 0x000...01 就是总留言数 1 条。
```

### B4. 命令行验证签名者恢复（对应证明 A3，密码学铁证）

```js
const txHash = "0x..............（填你的交易哈希）";
const tx = await ethers.provider.getTransaction(txHash);

// ethers 自带 recoverAddress，等价于节点内部 ecrecover
const signedTx = ethers.Transaction.from(tx);
signedTx.recoverAddress()   // 应该输出 = "0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266"
tx.from                     // 也等于这个地址

// 如果两者一样 → 证明 tx.from 账户的私钥确实签过这笔交易 → 身份验证通过 → 理所当然扣它的 gas
signedTx.recoverAddress().toLowerCase() === tx.from.toLowerCase()
// 应该返回 true  ✅
```

### B5. 计算 gas 费 = 余额差值（把 A1 的逻辑用数学等式证明出来）

```js
// 取 tx 和 receipt 后：
const gasFeeWei = tx.gasPrice * receipt.gasUsed;
ethers.formatEther(gasFeeWei);   // 比如 "0.000065247" ETH

// 这个数值应该 == 你交易后余额 - 交易前余额的绝对值（误差 ≤ 1 wei）
const balBefore = ethers.parseEther("10000");
const balAfter  = await ethers.provider.getBalance(addr);
const diff      = balBefore - balAfter;
diff === gasFeeWei   // 绝大多数情况下 true；如果恰好有转账/退gas可能有差异，但数量级完全一致
```

> 退出控制台：输入 `.exit` 回车。

---

## 四、讲述时推荐的「现场证明流程」（3 分钟搞定）

按这个顺序演示，现场观众能一眼跟上：

1. **进入快速模式** → 显示当前账户 ETH 余额是 `10000.000000` ETH；
2. **发一条留言** → 等待上链成功；
3. **指给观众看 3 个立刻变化的数字**：
   - ① 账户 ETH 余额变成 `9999.999...`（红色负数 = gas 费扣除）；
   - ② 下方出现「✅ 交易已上链」框，显示「区块 #N / gasUsed xxx / 状态成功」；
   - ③ 留言列表新增了那条留言；
4. **点交易哈希链接**，自动跳转区块浏览器，展示：
   - ④ 绿色「✅ 还原签名者地址 == 交易 from 地址，一致」+ 扣 gas 金额等式；
   - ⑤ 事件日志里能看到 author 和内容一模一样；
5. **（可选）切另一个账户**（比如账户 #1）再发一条，证明「每个账户独立扣自己的 ETH 当 gas」——这就把「谁签名谁付费」的因果关系坐实了。

> 以上 5 步就是对「怎么证明交易真实发生、凭什么扣该地址 ETH 当 gas」的完整可视化回答。
